/* Compatibility shim: enforcement is integrated into future-pick-protection.js. */
(function(){'use strict';console.info('[ACC Schedule Manager] legacy future-pick enforcement disabled; using unified protection.');})();
